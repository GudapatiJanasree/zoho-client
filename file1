
pipeline {
    agent any

    triggers {
        githubPush()
    }

    stages {
        stage('Build') {
            steps {
                echo "Build one triggered successfully!"
                sh 'date'
                sh 'whoami'
            }
        }
    }
}

